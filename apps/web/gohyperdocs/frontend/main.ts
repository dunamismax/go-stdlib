import '@picocss/pico/css/pico.min.css'
import './styles.css'

// Initialize HTMX when the page loads
document.addEventListener('DOMContentLoaded', () => {
  console.log('GoHyperDocs frontend initialized')
  
  // Add any TypeScript enhancements here
  // This is where you can add complex client-side interactions
  // that go beyond what HTMX can handle
})

// Export for potential use by other modules
export {}